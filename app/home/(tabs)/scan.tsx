import { View } from "react-native";
import React from "react";
import ScanIndex from "../../../components/Scan/ScanIndex";

const scan = () => {
  return (
    <View>
      <ScanIndex />
    </View>
  );
};

export default scan;
